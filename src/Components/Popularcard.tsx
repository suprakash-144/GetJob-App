import {Text, View} from 'react-native';
import React from 'react';

const Popularcard = ({item}: any) => {
  return (
    <View>
      <Text>{item.title}Popularcard</Text>
    </View>
  );
};

export default Popularcard;
